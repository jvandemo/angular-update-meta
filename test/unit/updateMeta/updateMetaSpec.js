'use strict';

describe('', function() {

  var module;

  beforeEach(function() {
    // Get module
    module = angular.module('updateMeta');
  });

  it('should exist', function() {
    expect(module).to.be.ok;
  });

});
